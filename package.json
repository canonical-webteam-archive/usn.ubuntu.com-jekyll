{
  "name": "usn.ubuntu.com",
  "version": "1.0.0",
  "description": "code for usn.ubuntu.com",
  "main": "index.js",
  "directories": {
    "example": "examples"
  },
  "scripts": {
    "test": "sass-lint static/**/*.scss --verbose --no-exit",
    "watch": "node-sass --include-path node_modules --source-map true --watch _sass --output css",
    "build": "node-sass --include-path node_modules _sass --output css",
    "clean": "rm -rf node_modules yarn-error.log css *.log *.sqlite _site/ build/ .jekyll-metadata"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/canonical-websites/usn.ubuntu.com.git"
  },
  "keywords": [
    "Ubuntu",
    "Security notices"
  ],
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/canonical-websites/usn.ubuntu.com/issues"
  },
  "homepage": "https://github.com/canonical-websites/usn.ubuntu.com#readme",
  "devDependencies": {
    "sass-lint": "^1.10.2",
    "node-sass": "^4.5.3",
    "vanilla-brochure-theme": "1.0.6"
  }
}
